name: AudioControl
options:
  bundleIdPrefix: com.justinstrong
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "26.3"
settings:
  base:
    SWIFT_VERSION: "5.0"
    SWIFT_STRICT_CONCURRENCY: minimal
    CURRENT_PROJECT_VERSION: 1
    MARKETING_VERSION: 1.0.0
targets:
  AudioControl:
    type: application
    platform: iOS
    sources:
      - path: AudioControl
    info:
      path: AudioControl/Info.plist
      properties:
        CFBundleDisplayName: AudioControl
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        ITSAppUsesNonExemptEncryption: false
        NSBluetoothAlwaysUsageDescription: AudioControl uses Bluetooth to find and configure your nearby ESP32 audio processor.
        NSMicrophoneUsageDescription: When you enable Agent Control and approve a nearby Mac, AudioControl can record the iPhone microphone for acoustic measurements and return the recording directly to that Mac.
        NSLocalNetworkUsageDescription: AudioControl uses your local network only when you enable Agent Control to communicate with a nearby Mac you approve.
        NSBonjourServices:
          - _ac-audio-dev._tcp
        UILaunchScreen: {}
    settings:
      base:
        DEVELOPMENT_TEAM: 3TKP8A48MF
        PRODUCT_BUNDLE_IDENTIFIER: com.justinstrong.AudioControl
        PRODUCT_NAME: AudioControl
        TARGETED_DEVICE_FAMILY: "1"
        CODE_SIGN_STYLE: Automatic
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS: YES
  AudioControlTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: AudioControlTests
    dependencies:
      - target: AudioControl
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.justinstrong.AudioControlTests
        GENERATE_INFOPLIST_FILE: YES
schemes:
  AudioControl:
    build:
      targets:
        AudioControl: all
        AudioControlTests: [test]
    test:
      targets:
        - AudioControlTests
